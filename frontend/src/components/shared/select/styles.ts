import React from "react";

export const SelecStyles:React.CSSProperties = {
    padding:"0.5rem",
    margin:"0.75rem",
    fontSize: "1rem",
    fontWeight: 900,
    borderRadius: "12px",
    border: "2px solid #e0ded0",
    backgroundColor: "#f1f0e8",
    color: "#333333",
    outline: "none",
    cursor: "pointer",
    width: "13rem",
    transition: "box-shadow 0.3s ease, border-color 0.3s ease, background-color 0.3s ease"
}

export const OptionsStyles:React.CSSProperties = {
    fontWeight:900,
}